#ifndef MATHS_H
#define MATHS_H

namespace maths {

    // largest distance in 

    float to_radians(float degrees);
    float to_degrees(float radians);

    bool approx(float a, float b);
}

#endif
